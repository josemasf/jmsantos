---
import MainLayout from "../layouts/MainLayout.astro";

import PorfolioPreview from "@components/PorfolioPreview.astro";

const porfolio = Object.values(
  import.meta.glob<{ frontmatter: { publishDate: string } }>("./porfolio/**/*.md", { eager: true })
)
  .filter(({ frontmatter }) => !!frontmatter.publishDate)
  .sort(
    (a, b) =>
      new Date(b.frontmatter.publishDate).valueOf() -
      new Date(a.frontmatter.publishDate).valueOf()
  );
---

<MainLayout
  title="Portfolio | José María Santos"
  description="Proyectos de frontend, arquitectura y producto de José María Santos"
>
  <main id="main-content" class="container mx-auto px-6 py-16 md:px-10 md:py-24">
    <p class="section-kicker">Trabajo seleccionado</p>
    <h1 class="mt-3 font-display text-5xl font-bold text-slate-900 dark:text-white">Portfolio</h1>
    <p class="mt-5 max-w-2xl text-lg leading-relaxed text-slate-600 dark:text-slate-300">Productos y experimentos donde exploro interfaces, arquitectura y experiencias digitales.</p>
    <div class="mt-12 grid grid-cols-1 gap-6 md:grid-cols-2 xl:grid-cols-3">
      {porfolio.map((project) => <PorfolioPreview presentation={project} />)}
    </div>
  </main>
</MainLayout>
